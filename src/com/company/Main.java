package com.company;

import java.io.*;
import java.util.*;
import java.text.*;
import java.math.*;
import java.util.regex.*;

public class Main {

    public static void main(String[] args) {
     /*   Scanner in = new Scanner(System.in);
        int m = in.nextInt();
        int n = in.nextInt();
        int wordCounter = 0;

        HashMap<String, Integer> magazineMap = new HashMap<>();
        HashMap<String, Integer> ransomMap = new HashMap<>();
        ArrayList<Integer> arr = new ArrayList<>();
        Set<String> set = new HashSet<>();



        String magazine[] = new String[m];
        for(int magazine_i=0; magazine_i < m; magazine_i++){
            magazine[magazine_i] = in.next();
            magazineMap.put(magazine[magazine_i], magazineMap.containsKey(magazine[magazine_i]) ? magazineMap.get(magazine[magazine_i])+1 : 1 );
        }
        String ransom[] = new String[n];
        for(int ransom_i=0; ransom_i < n; ransom_i++){
            ransom[ransom_i] = in.next();
            ransomMap.put(ransom[ransom_i], ransomMap.containsKey(ransom[ransom_i]) ? ransomMap.get(ransom[ransom_i])+1 : 1);
        }

        Iterator ran = ransomMap.keySet().iterator();
        int ransom_value;
        int magazine_value;
        String value;

        while(ran.hasNext()){
            value = (String) ran.next();
            ransom_value = ransomMap.get(value);
            magazine_value = magazineMap.containsKey(value) ? magazineMap.get(value) : -1;

            if( (magazine_value != -1) && (ransom_value <= magazine_value)){
                wordCounter = wordCounter + ransom_value;
            }
        }

        if(wordCounter == ransom.length){
            System.out.print("Yes");
        }else System.out.print("No");


    }*/
       /*
     HashMap<String, Integer> map = new HashMap<String, Integer>();
     map.put("hi", 1);
     map.put("bye", 2);
     int i = map.get("hi");
        System.out.println(i);
     for(Map.Entry<String, Integer> m: map.entrySet()){
        System.out.println(m.getKey()+" "+m.getValue());
     }*/


    }
}
